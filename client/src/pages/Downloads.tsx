import { Button } from "@/components/ui/button";
import { ArrowRight, CheckCircle2, Download, Laptop, ShieldCheck, Smartphone } from "lucide-react";
import { useLocation } from "wouter";

function isAndroid() {
  return typeof navigator !== "undefined" && /Android/i.test(navigator.userAgent);
}

export default function Downloads() {
  const [, setLocation] = useLocation();
  const android = isAndroid();
  return <div className="min-h-screen bg-[#05060b]"><div className="grid-backdrop pointer-events-none absolute inset-x-0 top-0 h-[620px]" /><header className="container relative z-10 flex items-center justify-between py-5"><button onClick={() => setLocation("/")} className="font-display text-lg font-bold">PROCTOR<span className="text-pink-400">X</span></button><Button variant="outline" onClick={() => setLocation("/")} className="border-cyan-200/25 text-cyan-100">Back to website</Button></header><main className="container relative z-10 py-12 sm:py-20"><div className="max-w-3xl"><p className="tech-label">ProctorX downloads</p><h1 className="mt-3 font-display text-4xl font-bold tracking-tight text-white sm:text-6xl">Use ProctorX on the device that fits your assessment.</h1><p className="mt-5 max-w-2xl text-base leading-7 text-slate-300">The website works on desktop, Chromebook, Android, and modern mobile browsers. Android APK releases are published only after signing and physical-device validation.</p></div><div className="mt-12 grid gap-5 lg:grid-cols-2"><section className={`hud-panel p-6 sm:p-8 ${android ? "border-cyan-300/45" : ""}`}><div className="flex items-start justify-between"><Smartphone className="h-7 w-7 text-cyan-200" /><span className="tech-label text-[0.55rem]">{android ? "Android detected" : "Android"}</span></div><h2 className="mt-8 font-display text-2xl font-semibold">Android app</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">A signed release APK is not available yet. The current native wrapper is in production-domain and physical-device validation, so no placeholder build is offered for download.</p><div className="mt-6 border border-pink-400/25 bg-pink-400/8 p-4 text-xs leading-5 text-pink-100"><p className="font-semibold">Release gate in progress</p><p className="mt-1">The download button will appear here only after the APK is signed, pointed at the published HTTPS site, and tested with candidate-controlled camera and microphone prompts.</p></div><Button disabled className="mt-7 w-full bg-slate-700 text-slate-300"><Download className="mr-2 h-4 w-4" />Signed APK pending</Button></section><section className="hud-panel p-6 sm:p-8"><div className="flex items-start justify-between"><Laptop className="h-7 w-7 text-pink-300" /><span className="tech-label text-[0.55rem]">Desktop &amp; web</span></div><h2 className="mt-8 font-display text-2xl font-semibold">Open in your browser</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">APK files run on Android only. On Windows, macOS, Linux, Chromebook, iPhone, or iPad, use the secure web application. Where your browser supports it, you can install the site as a PWA from the browser menu.</p><Button onClick={() => setLocation("/signin")} className="neon-button mt-7 w-full bg-pink-400 text-slate-950 hover:bg-pink-300">Open secure web app <ArrowRight className="ml-2 h-4 w-4" /></Button></section></div><section className="mt-8 grid gap-4 border border-cyan-200/15 bg-white/[.02] p-6 sm:grid-cols-3"><div><ShieldCheck className="h-5 w-5 text-cyan-200" /><p className="mt-3 text-sm font-semibold">Consent stays local</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Device prompts remain controlled by the candidate.</p></div><div><CheckCircle2 className="h-5 w-5 text-cyan-200" /><p className="mt-3 text-sm font-semibold">No placeholder APK</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Only a validated, signed release is published.</p></div><div><Laptop className="h-5 w-5 text-cyan-200" /><p className="mt-3 text-sm font-semibold">Desktop ready</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Use the browser or install the available web-app shortcut.</p></div></section></main></div>;
}
